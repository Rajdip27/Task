@model PIISTECHLTD.Application.ViewModel.OrderVm

@{
    ViewData["Title"] = "Shipper Create";
}

<br />
<br />

<div class="row mt-5">
    <div class="col-8 mx-auto">
        <div class="card">
            <div class="card-header">
                <h4 class="card-title">@ViewBag.Title</h4>
            </div>
            <div class="card-body">
                <form asp-controller="Order" asp-action="CreateOrEdit" method="post">
                    <div class="form-group row mt-2">
                        <label asp-for=ProductName class="col-form-label col-md-2"></label>
                        <div class="col-md-7">
                            <input type="text" asp-for="ProductName" class="form-control">
                        </div>
                    </div>
                    <div class="form-group row mt-2">
                        <label asp-for=OrderDate class="col-form-label col-md-2"></label>
                        <div class="col-md-7">
                            <input type="text" asp-for="OrderDate" class="form-control">
                        </div>
                    </div>
                    <div class="form-group row mt-2 ">
                        <label asp-for=Address class="col-form-label col-md-2"></label>
                        <div class="col-md-10">
                            <textarea type="text" asp-for="Address" class="form-control"></textarea>
                        </div>
                    </div>
                    <div class="input-group-append mt-4 justify-content-end">
                        <input type="submit" value="Save" class="btn btn-primary text-center mr-2" />
                        <a class="btn btn-danger text-center" asp-action="Index" asp-controller="Shipper">Back to List</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>


@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}